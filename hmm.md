Lang
	config (require)
Bot
	lang (direct)
	config (require)
Perms
	config (require)
Config
	bot (direct)
	perms (direct)
	lang (direct)
Server
	config (direct)
	lang (direct)
	bot (direct)
	oauth (require)
OAuth
	server (direct)
	bot (direct)